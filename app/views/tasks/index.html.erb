<h1>任務管理</h1>
<%= link_to "新增任務",new_task_path %>

<table>
  <thead>
   <tr>
       <td>任務名稱</td>
       <td>任務內容</td>
   </tr>
  </thead>
  <tbody>
    <% @tasks.each do |task| %>
      <td><%= task.title %></td>
      <td><%= task.content %></td>
  </tbody>
  <% end %>
</table>
 